# Common paths and names
middleware_home: '{{ oracle_base_path }}/product/Oracle_Home'
middleware_common_home: '{{ middleware_home }}/oracle_common'
domains_home: '{{ oracle_base_path }}/config/Domains'
applications_home: '{{ oracle_base_path }}/config/applications'
mw_installer_folder: '{{ oracle_base_path }}/installer'
domain_home: '{{ domains_home }}/{{ domain_name }}'
nodemanager_home: '{{ oracle_base_path }}/config/nodemanagers/{{ domain_name }}'
weblogic_home: '{{ middleware_home }}/wlserver'
nodemanager_username: 'weblogic'
weblogic_admin: 'weblogic'
admin_server_name: 'AdminServer'
server_hostname: '{{ ansible_fqdn }}'
